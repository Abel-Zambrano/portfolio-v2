import React from "react"
import styled from "styled-components"

import bioData from "../JS/bio"

const StyledBio = styled.article``

console.log(bioTitle)

const Bio = () => {
  return (
    <StyledBio>
      <h2>{}</h2>
      <p>{}</p>
    </StyledBio>
  )
}

export default Bio
